@model ATOMv0.Models.DimFinancialCompany
@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>


@using (Html.BeginForm("Create", "DimFinancialCompanies", FormMethod.Post, new { @class = "form-crud" }))
{
    @Html.AntiForgeryToken()

    <div>
        <h2>Create</h2>
        <h4>DimFinancialCompany</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.FinancialCompanyName, new { @class = "control-label" })
            <div>
                @Html.EditorFor(model => model.FinancialCompanyName)
                @Html.ValidationMessageFor(model => model.FinancialCompanyName)
                <br /><br />
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-1 col-md-10">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </div>
    </div>

    <div>
        @Html.ActionLink("Back to List", "Index")
    </div>
}



@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}